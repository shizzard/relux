test "echo and match" {
    shell s {
        ~5s
        > echo hello-relux
        <? ^hello-relux$
        match_ok()

        > echo "value=42"
        <? ^value=(\d+)$
        match_ok()
    }
}
