test "always invalid" {
    """
    Fail at resolve time due to a non-existent effect dependency.
    """
    start NonExistent as S
    shell S.s {
        > echo unreachable
        <? ^unreachable$
        match_ok()
    }
}
