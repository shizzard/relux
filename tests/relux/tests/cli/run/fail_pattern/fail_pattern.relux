test "run reports fail pattern triggered" {
    """
    Verify relux run reports when a fail pattern matches output.
    """
    shell s {
        > relux run --manifest ${__RELUX_TEST_ROOT}/fixtures/Relux.toml 2>&1
        <? fail pattern matched
        match_prompt()
        match_exit_code(1)
    }
}
