test "flaky marker skips the test" {
    """
    Verify flaky marker causes the test to be reported as skipped.
    """
    shell s {
        > cd ${__RELUX_TEST_ROOT}/fixtures && relux run 2>&1
        <? 1 skipped
        match_ok()
    }
}
