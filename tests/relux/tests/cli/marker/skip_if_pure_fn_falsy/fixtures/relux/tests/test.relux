pure fn always_empty() {
    ""
}

[skip if always_empty()]
test "conditional" {
    """
    Do not skip when the pure function returns an empty (falsy) value.
    """
    shell s {
        > echo hello
        <? ^hello$
    }
}
