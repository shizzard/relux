# skip
effect Skipped {
    shell s {
        > echo "effect ran"
        <? ^effect ran$
    }
}

test "depends on skipped effect" {
    """
    Depend on a skipped effect.
    """
    start Skipped as s
    shell s {
        > echo "should not run"
        <? ^should not run$
    }
}
