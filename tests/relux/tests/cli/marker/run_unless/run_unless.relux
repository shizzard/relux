test "run unless: skips when var is set" {
    """
    Verify run-unless marker skips the test when the condition variable is set.
    """
    shell s {
        > MY_VAR=1 relux run --manifest ${__RELUX_TEST_ROOT}/fixtures/Relux.toml 2>&1
        <? 1 skipped
        match_ok()
    }
}

test "run unless: runs when var is unset" {
    """
    Verify run-unless marker runs the test when the condition variable is unset.
    """
    shell s {
        > relux run --manifest ${__RELUX_TEST_ROOT}/fixtures/Relux.toml 2>&1
        <? 1 passed
        match_ok()
    }
}
