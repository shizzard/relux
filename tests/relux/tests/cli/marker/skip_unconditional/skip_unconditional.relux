test "unconditional skip always skips" {
    """
    Verify bare skip marker unconditionally skips the test.
    """
    shell s {
        > relux run --manifest ${__RELUX_TEST_ROOT}/fixtures/Relux.toml 2>&1
        <? 1 skipped
        match_ok()
    }
}
