pure fn always_true() {
    "yes"
}

[run if always_true()]
test "conditional" {
    """
    Run only if the pure function returns a truthy value.
    """
    shell s {
        > echo hello
        <? ^hello$
    }
}
