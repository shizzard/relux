test "invalid operator is a parse error" {
    """
    Verify relux check rejects an invalid operator in a shell block.
    """
    shell s {
        > ${__RELUX} check --manifest ${__RELUX_TEST_ROOT}/fixtures/Relux.toml 2>&1
        <? expected
        match_not_ok(1)
    }
}
