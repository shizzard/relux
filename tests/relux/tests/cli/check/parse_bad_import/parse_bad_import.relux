test "import with no path is a parse error" {
    """
    Verify relux check rejects an import statement without a module path.
    """
    shell s {
        > relux check --manifest ${__RELUX_TEST_ROOT}/fixtures/Relux.toml 2>&1
        <? unexpected token
        match_prompt()
        match_exit_code(1)
    }
}
