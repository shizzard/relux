test "check detects alias casing mismatch" {
    """
    Verify relux check reports when an import alias changes casing kind.
    """
    shell s {
        > ${__RELUX} check --manifest ${__RELUX_TEST_ROOT}/fixtures/Relux.toml 2>&1
        <? expected function name
        match_not_ok(1)
    }
}
