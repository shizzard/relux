test "check detects wrong arity" {
    """
    Verify relux check reports a call with the wrong number of arguments.
    """
    shell s {
        > ${__RELUX} check --manifest ${__RELUX_TEST_ROOT}/fixtures/Relux.toml 2>&1
        <? undefined function
        match_prompt()
        match_exit_code(1)
    }
}
