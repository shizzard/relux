test "check detects duplicate definition" {
    """
    Verify relux check reports a duplicate function definition via imports.
    """
    shell s {
        > ${__RELUX} check --manifest ${__RELUX_TEST_ROOT}/fixtures/Relux.toml 2>&1
        <? name conflict
        match_not_ok(1)
    }
}
