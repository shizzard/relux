test "missing closing brace is a parse error" {
    """
    Verify relux check rejects a test body with a missing closing brace.
    """
    shell s {
        > cd ${__RELUX_TEST_ROOT}/fixtures && relux check 2>&1
        <? unexpected token
        match_prompt()
        match_exit_code(1)
    }
}
