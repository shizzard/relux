test "check detects circular import" {
    """
    Verify relux check reports circular module imports.
    """
    shell s {
        > cd ${__RELUX_TEST_ROOT}/fixtures && relux check 2>&1
        <? circular import
        match_prompt()
        match_exit_code(1)
    }
}
