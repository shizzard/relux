effect A {
    start B as Dep
    shell s {
        > echo a
    }
}

effect B {
    start A as Dep
    shell s {
        > echo b
    }
}

test "cycle" {
    start A as X
    shell X.s {
        > echo hello
    }
}
