effect A {
    start B as dep
    shell s {
        > echo a
    }
}

effect B {
    start A as dep
    shell s {
        > echo b
    }
}

test "cycle" {
    start A as a
    shell s {
        > echo hello
    }
}
