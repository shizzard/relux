test "check detects module not found" {
    """
    Verify relux check reports an import of a nonexistent module.
    """
    shell s {
        > cd ${__RELUX_TEST_ROOT}/fixtures && relux check 2>&1
        <? module not found
        match_prompt()
        match_exit_code(1)
    }
}
