test "check passes when sub-start resolves in effect home scope" {
    """
    Verify that an effect's sub-starts resolve in the effect's home module,
    not in the test module that uses the effect. The test file imports only
    Composite, which internally starts Base — Base is imported in
    lib/composite but NOT in the test file.
    """
    shell s {
        > ${__RELUX} check --manifest ${__RELUX_TEST_ROOT}/fixtures/Relux.toml 2>&1
        <? check passed
        match_ok()
    }
}
