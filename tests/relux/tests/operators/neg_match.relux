test "negative regex match succeeds when pattern absent" ~300ms {
    """
    Verify negative regex match passes when the pattern never appears.
    """
    shell s {
        ~100ms
        > echo hello
        <? ^hello$
        <!? never_appears
    }
}

test "negative literal match succeeds when text absent" ~300ms {
    """
    Verify negative literal match passes when the text never appears.
    """
    shell s {
        ~100ms
        > echo hello
        <? ^hello$
        <!= never_appears
    }
}

test "negative match does not advance cursor" ~300ms {
    """
    Verify a successful negative match leaves the cursor in place,
    allowing subsequent positive matches to find later output.
    """
    shell s {
        ~100ms
        > echo first
        > echo second
        <? ^first$
        <!? no_such_thing
        <? ^second$
    }
}

test "negative regex match with variable interpolation" ~300ms {
    """
    Verify variables are interpolated in negative regex payloads.
    """
    shell s {
        ~100ms
        let bad = "error_pattern"
        > echo "all good"
        <? ^all good$
        <!? ${bad}
    }
}

test "negative literal match with special chars" ~300ms {
    """
    Verify negative literal match treats regex metacharacters as plain text.
    """
    shell s {
        ~100ms
        > echo "price is 10"
        <? ^price is 10$
        <!= $10.00 (USD)
    }
}

test "negative match returns empty string" ~300ms {
    """
    Verify capturing a negative match result yields an empty string.
    """
    shell s {
        ~100ms
        > echo hello
        <? ^hello$
        let result = <!? absent_pattern
        > echo "got '${result}'"
        <? ^got ''$
    }
}
